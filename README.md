# to run the ayurveda inventory first create .env.local file in ayurveda-inventory folder ( root folder )
# then run "bun i" then "npx prisma dev" in one terminal and then copy the database_url and shadow_database_url into the .env.local folder
# then run bun dev in another terminal  to locally run the application 
