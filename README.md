# to run the ayurveda inventory first create .env.local file in ayurveda-inventory folder ( root folder )
# then run "npx prisma dev" in one terminal and then copy the database_url and shadow_database_url into the .env.local folder
# then open another terminal and run bun i to install dependencies
# then run bun dev to locally run the application 
